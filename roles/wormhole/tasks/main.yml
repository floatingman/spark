---
- name: Install magic-wormhole
  aur: name={{ item }} user={{ user.name }}
  with_items:
      - python-hkdf
      - python-spake2
      - python-ipaddress
      - python-txtorcon
      - magic-wormhole
  tags:
    - aur

