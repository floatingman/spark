---
- name: Install magic-wormhole
  aur: name={{ item }} user={{ user.name }}
  with_items:
      - python-hkdf
      - python-spake2
      - python-humanize
      - python-tqdm
      - python-ipaddress
      - magic-wormhole
  tags:
    - aur

